# hierarchical-md-rag 런타임 의존성.
#
# 이 저장소는 원래 팀 프로젝트(AI_7-team)와 공유하는 conda 환경(langc)에 기대어
# 별도 requirements.txt 없이 개발됐다(pyproject.toml 참고). GCP 등 새 환경에
# 깨끗하게 재현하려면 별도 파일이 필요해 추가한다 — src/·scripts/ 전체의 실제
# import 문을 훑어 만들었고(내부 모듈·표준 라이브러리 제외), 버전은 지금 langc
# 환경에서 실제로 검증된 값 그대로 고정했다(`pip freeze` 대조).
#
# 제외한 것:
# - pytesseract: workflow.py에서 try/except로 감싼 선택적 OCR 경로에서만 쓰고,
#   langc 환경에도 실제로 설치돼 있지 않다 — 그 경로가 지금까지 문제없이 동작했다는
#   뜻이라 필수 의존성이 아니다.
# - parser_bridge / preprocessor: pip 패키지가 아니라 DB 재색인 스크립트
#   (scripts/build_index_new_parser.py 등)가 참조하는 이웃 저장소 모듈 — 이미
#   만들어진 data_index/chroma_B를 그대로 질의만 하는 배포 경로(scripts/gradio_app.py,
#   scripts/api.py)에는 필요 없다.

# --- 검색/DB ---
chromadb==1.5.9
sentence-transformers==6.0.0
pandas==3.0.2
openpyxl==3.1.5  # pandas.read_excel의 .xlsx 엔진 — 존재 게이트 근거 파일(나라장터_RFP_메타데이터.xlsx) 읽기에 필요
kiwipiepy==0.23.1
PyYAML==6.0.3

# --- LLM/생성 ---
langchain==1.3.16
langchain-core==1.6.0
langchain-openai==1.1.12
langchain-text-splitters==1.1.2
openai==2.30.0
pydantic==2.12.5

# --- 문서 파싱 ---
pymupdf==1.28.2
pymupdf4llm==0.0.17
pillow==11.3.0

# --- 서비스/배포 ---
fastapi==0.135.3
uvicorn==0.43.0
gradio==6.26.0
python-dotenv==1.2.2

# --- 테스트(배포에는 불필요, 로컬 개발 시에만) ---
pytest==9.1.1
