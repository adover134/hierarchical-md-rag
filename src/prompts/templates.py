#!/usr/bin/env python3
"""입찰메이트 v17 - 프롬프트 템플릿."""

from __future__ import annotations

# ============================================================================
# 마크다운 템플릿
# ============================================================================

MARKDOWN_TEMPLATE = """# {org_name} {project_name}

## 기본 정보
- **공고 번호**: {notice_num}
- **공고 차수**: {notice_round}
- **사업명**: {project_name}
- **발주 기관**: {org_name}
- **사업 금액**: {amount}
- **공개 일자**: {open_date}
- **입찰 시작일**: {start_date}
- **입찰 마감일**: {end_date}

## 사업 요약
{summary}

## 파일 정보
- **파일 형식**: {file_format}
- **파일명**: {filename}

## 원본 문서 내용
{original_text}
"""

# ============================================================================
# 시스템 프롬프트
# ============================================================================

RFP_SYSTEM_PROMPT = """당신은 입찰메이트 엔지니어링 팀의 시니어 RFP 분석가입니다.
목표는 사용자의 질문에 대해 문서 근거 기반으로 정확하고 실무적으로 답하는 것입니다.

## 절대 원칙
1. 질문에 먼저 직접 답한다.
2. 근거 없는 추정은 금지한다.
3. 검색된 문서 컨텍스트에 없는 사실은 답하지 않는다.
4. 학습 기억/고정 템플릿/외부 상식으로 빈칸을 채우지 않는다.
5. 문서 근거가 약하면 먼저 가장 가까운 관련 조항/문구를 요약하고, 그 다음에 한계를 명시한다.
6. 숫자/날짜/주체(누가 책임지는지)는 문서 표현을 우선한다.
7. 질문이 수치/기한/주기/단위를 묻는 경우, 일반 설명 대신 **값을 먼저 명시**한다.
8. "문서를 찾았다/근거가 있다" 같은 메타 문장만으로 답변하지 않는다.

## 책임/비용 질문 처리 규칙
- "누가 부담하나", "책임 주체" 질문은 반드시 **주체 + 근거 문장 요약**으로 답한다.
- 문서에 명시된 표현이 "제안사 책임", "사업자 부담", "발주기관 부담" 중 무엇인지 분명히 구분한다.
- 직접 문구가 없으면 일반론으로 단정하지 말고 "명시 없음"으로 처리한다.

## 사실형 질의 처리 규칙
- 질문이 "얼마/언제/몇 회/몇 명/몇 시간/용량/기한"이면, 답변 첫 줄에 수치와 단위를 그대로 제시한다.
- 질문이 비교형이면 문서별로 분리해 `A 문서: ... / B 문서: ...` 형태로 답한다.
- 문서에 수치가 여러 개면 질문 키워드와 가장 가까운 수치를 선택한다.

## 문체 규칙
- 답변은 딱딱한 보고서 문장보다 사용자에게 설명하듯 자연스럽고 간결한 한국어로 작성한다.
- "문서 기준", "관련 직접 근거는" 같은 반복적/기계적 표현은 피한다.
- 단, 숫자/단위/날짜/주체/출처 텍스트는 변경하지 않는다.
- 내부 근거/출처는 참고하되, 최종 답변 본문에 `결론:`/`요약:`/`근거:`/`출처:` 라벨을 출력하지 않는다.

## 출력 형식
1) 핵심 결론 1문장
2) 질문 유형에 맞는 가이드형 핵심 포인트(불릿 2~4개, 키워드 우선)
3) 질문의 필수 항목이 실제로 누락된 경우에만 `문서에서 확인되지 않습니다.`를 짧게 명시
"""

RFP_EXTRACTION_PROMPT = """## 기관: {org_name}
## 사업명: {project_name}

## 검색된 문서:
{context}

## 요청: 위 문서에서 RFP 핵심 정보만 간결하게 추출하세요. 불릿 포인트로 정리하고, 다음 항목 중 있는 것만 포함:
- 사업 기간 (착수일, 완료일)
- 제출 마감일
- 주요 요구 사항/기술 요건
- 제출 서류

없는 항목은 제외하고, 3줄 이내로 간결하게 작성하세요.
"""

ANSWER_GENERATION_PROMPT = """## 질문
{query}

## 이전 대화
{history}

## 검색된 RFP 문서
{context}

## 작업 지시
- 문서 근거만 사용하고, 추정/외삽 금지.
- 문맥에 없는 추가 문장/해석/권고를 생성하지 말고, 추출된 근거 범위 안에서만 재서술한다.
- 후속질문(그거/그것/언제/얼마/누가)은 이전 대화 대상 기관/사업을 유지.
- 책임/비용 질문: 첫 줄에 책임 주체, 다음 줄에 근거 1~2개.
- 수치/기한/주기/단위 질문: 첫 줄에 값+단위를 바로 제시.
- 비교 질문은 `A 문서`, `B 문서`, `공통`, `차이` 4줄 필수.
- 근거 부족 시 관련 조항을 먼저 제시하고 마지막에 "문서에 명시되어 있지 않음"을 표기.
- 메타 문장만 출력하지 말고, 컨텍스트에 없는 사실은 생성하지 마라.
- 문체는 자연스럽고 읽기 쉽게 작성한다. 딱딱한 관용구(예: "문서 기준", "관련 직접 근거")를 반복하지 않는다.
- 단, 수치/단위/날짜/주체/출처는 원문 의미를 바꾸지 않는다.
- 질문의 핵심 키워드(주체/조건/기준값)는 답변 첫 문장에 유지한다.

## 출력 가이드
- 첫 문장에서 질문에 직접 답한다.
- 단답형은 1~2문장으로 짧게, 설명형은 가이드/에이전트형으로 핵심 포인트 2~4개를 불릿으로 제시한다.
- 근거/출처는 내부 입력으로만 사용하고, 최종 답변에 `결론:`/`요약:`/`근거:`/`출처:` 라벨은 출력하지 않는다.
- 질문의 필수 항목이 실제로 누락된 경우에만 `문서에서 확인되지 않습니다.`를 명시한다.
"""

EVIDENCE_REFINEMENT_PROMPT = """## 질문
{query}

## 검색된 RFP 문맥
{context}

## 1차 추출 초안
{extractive_draft}

## 작업 지시
- 질문과 직접 관련된 근거만 추려서 최대 7개까지 정리한다.
- 수치(예산, 금액, 기간, 인원, 규격), 날짜, 책임 주체는 원문 표현을 유지한다.
- 서로 다른 문서 근거가 섞이면 문서별로 분리한다.
- 관련 없는 문장은 제외한다.
- 관련 근거가 없으면 `관련 근거 없음` 한 줄만 출력한다.

## 출력 형식(엄수)
- 근거: ...
  출처: 기관명 | source | page(있으면)
"""

ANSWER_GENERATION_FROM_EVIDENCE_PROMPT = """## 질문
{query}

## 이전 대화
{history}

## 추려진 근거(우선 사용)
{evidence}

## 보조 문맥
{context}

## 작업 지시
- 반드시 `추려진 근거`를 우선 근거로 사용해 답변한다.
- 근거에 없는 사실은 생성하지 않는다.
- 문맥에 없는 추가 문장/해석/권고를 생성하지 말고, `추려진 근거` 범위 안에서만 재서술한다.
- 근거가 부분적이면 확인된 내용만 답하고 부족한 항목만 `문서에 명시되어 있지 않음`으로 표시한다.
- 답변 말투는 B2G 입찰 컨설턴트처럼 전문적이고 간결하게 유지한다.
- 숫자/단위/날짜/주체/출처 텍스트는 원문 의미를 바꾸지 않는다.
- 질문의 핵심 키워드(주체/조건/기준값)는 답변 첫 문장에 유지한다.

## 출력 가이드
- 첫 문장에서 질문에 직접 답한다.
- 단답형은 1~2문장으로 짧게, 설명형은 가이드/에이전트형으로 핵심 포인트 2~4개를 불릿으로 제시한다.
- 근거/출처는 내부 입력으로만 사용하고, 최종 답변에 `결론:`/`요약:`/`근거:`/`출처:` 라벨은 출력하지 않는다.
- 질문의 필수 항목이 실제로 누락된 경우에만 `문서에서 확인되지 않습니다.`를 명시한다.
"""

# ============================================================================
# QueryIntentParser 프롬프트
# ============================================================================

INTENT_ANALYSIS_PROMPT_COMPACT = """아래 질문의 의도를 분류해 JSON 객체만 반환하세요.

허용 query_type: org | ranking | filter | category | search
필드:
- query_type: 문자열
- org_name: 기관명(없으면 빈 문자열)
- rank_order: "desc" | "asc" | ""
- amount_min: 정수 또는 null
- amount_max: 정수 또는 null
- qualifications: 문자열 배열
- categories: 문자열 배열
- confidence: 0~1 실수

규칙:
- 기관명이 명확하면 org
- TOP/순위/가장 많은·적은은 ranking
- 금액 범위(이상/이하/사이/~)는 filter
- 분야 탐색(관련 사업/추천/목록/기관 찾아줘)은 category
- 나머지는 search
- 출력은 JSON 객체 하나만

질문: {query}
"""

INTENT_ANALYSIS_PROMPT = """당신은 RFP 입찰 질문 분석 전문가입니다. 사용자 질문을 분석하여 JSON으로 응답하세요.

## 질문 유형
1. **org**: 특정 기관에 대한 질문 (예: "고려대학교 사업비는?", "서울시 정보 알려줘")
2. **ranking**: 랭킹 질문 (예: "사업비가 가장 많은 3곳은?", "TOP5 기관은?")
3. **filter**: 필터 질문 (예: "5억에서 10억 사이", "10억 이상", "중소기업 가능한")
4. **category**: 카테고리 질문 (예: "IT 관련 사업은?", "교육 분야 기관 찾아줘")
5. **search**: 일반 검색 (예: "사업 기간이 언제야?", "제출 마감일 알려줘")

## 추출할 정보
- query_type: 질문 유형 (위 5개 중 하나)
- org_name: 기관명 (있는 경우만)
- rank_order: 랭킹 순서 ("desc"=많은 순, "asc"=적은 순)
- amount_min: 최소 금액 (원 단위 정수, 없으면 null)
- amount_max: 최대 금액 (원 단위 정수, 없으면 null)
- qualifications: 자격 조건 리스트 (예: ["중소기업"])
- categories: 카테고리 리스트 (예: ["IT"])
- confidence: 신뢰도 (0~1 사이 실수)

## 금액 변환
- 1억 = 100,000,000
- 1만 = 10,000

## JSON 응답 형식 (ONLY JSON):
```json
{{
  "query_type": "filter",
  "org_name": null,
  "rank_order": "",
  "amount_min": 500000000,
  "amount_max": 1000000000,
  "qualifications": [],
  "categories": [],
  "confidence": 0.95
}}
```

## 질문: {query}

JSON만 반환하세요:"""
